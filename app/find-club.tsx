import { ClubMatchmaking } from "@/components/club/ClubMatchmaking";

/** Route inchangée (header natif via app/_layout.tsx) — contenu extrait dans ClubMatchmaking (Étape 5). */
export default function FindClubScreen() {
  return <ClubMatchmaking />;
}
